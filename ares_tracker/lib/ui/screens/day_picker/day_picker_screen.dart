import 'package:flutter/material.dart';

class DayPickerScreen extends StatelessWidget {
  const DayPickerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Day Picker')),
    );
  }
}
